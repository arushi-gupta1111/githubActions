name: Hello GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Fancy Hello
        run: echo "Hello, GitHub Actions!!!!!!!Arushi!"
##thi is my first githubActions project
